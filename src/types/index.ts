export interface Car {
  id: number;
  name: string;
  price: string;
  engine: string;
  power: string;
  transmission: string;
  mileage: string;
  features: string[];
}

export interface User {
  id: string;
  email: string;
  name: string;
}